function multiplicaArray(numeros, numero){

    return numeros.map(elemento=> elemento * numero);
}
let numeros = [1, 2, 3, 4, 5];
let numero = 5;

console.log(multiplicaArray(numeros, numero));
    
    
